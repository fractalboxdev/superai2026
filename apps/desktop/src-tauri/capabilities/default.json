{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window talks only to the app's own commands and events.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
