{
  "name": "landing",
  "type": "module",
  "version": "0.0.0",
  "private": true,
  "engines": {
    "node": ">=22.13"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && node scripts/build-slides.mjs",
    "build:slides": "node scripts/build-slides.mjs",
    "preview": "astro preview",
    "arts:generate": "node scripts/generate-arts.mjs",
    "lint": "astro check",
    "typecheck": "astro check",
    "clean": "rm -rf dist .astro"
  },
  "dependencies": {
    "@superai2026/design-system": "workspace:*",
    "astro": "^5.2.5"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.4",
    "ai": "^5.0.0",
    "sharp": "^0.34.5"
  }
}
