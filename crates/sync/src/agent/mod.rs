//! Sandbox agents (spec 04): the agent runtime loop, the swappable inference
//! backend, and the editor agent that answers questions inside relay docs.

pub mod editor;
pub mod inference;
pub mod runtime;
