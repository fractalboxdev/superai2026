{
  "name": "@superai2026/infra",
  "version": "0.0.0",
  "private": true,
  "description": "Contextful cloud-side Pulumi recipes (spec 07 §5). Spec-only: provision with `pulumi preview`/`up` after providing provider credentials.",
  "main": "index.ts",
  "devDependencies": {
    "@pulumi/aws": "^6",
    "@pulumi/command": "^1",
    "@pulumi/pulumi": "^3",
    "@pulumi/tailscale": "^0.17",
    "@pulumiverse/vercel": "^1",
    "typescript": "^5.7.3"
  }
}
